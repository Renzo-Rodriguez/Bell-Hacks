#need to
#probably a csv
import pandas as pan 
import numpy as np 
import os 
import matplotlib as plt


topology = Image.open("path-to-image").convert('RGBA')
topology = np.array(topology)

percipitation = Image.open


class data:
    def __init__(self,data,start:tuple,end:tulple) -> None:
        self.data = data
    def shrink(self,amount):
        length = self.data.shape[0]
        width = 

        self.data = 
    #this depends on the current data type, but if we consider all of the data 


class dataman:
    def __init__(self):
        self.data = {}

    def loadimage(path-to-image:str,name:str,location:tuple) -> None:

        self.data[name] = data(np.array(Image.open(path-to-image)))
    #we can add an option to load a different thing
    def

